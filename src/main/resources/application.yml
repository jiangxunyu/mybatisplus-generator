#��������Դ
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://172.16.90.221:3306/dl?useUnicode=true&useSSL=false&characterEncoding=utf8
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:erongdu.com}
    # ��ʼ������
    druid:
      initial-size: 3
      # ��С������
      min-idle: 3
      # ���������
      max-active: 15
      # ��ȡ���ӳ�ʱʱ��
      max-wait: 5000
      # ������Ч�Լ��ʱ��
      time-between-eviction-runs-millis: 90000
      # ������ʱ��
      min-evictable-idle-time-millis: 1800000
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      validation-query: select 1
      # ���ü��ͳ�����ص�filters
      filters: stat
      stat-view-servlet:
        url-pattern: /api/druid/*
        reset-enable: false
      web-stat-filter:
        url-pattern: /*
        exclusions: "*.js,*.gif,*.jpg,*.bmp,*.png,*.css,*.ico,/api/druid/*"
